{% extends 'adminBase.html.twig' %}

{% block content %}
    <h1>Promotion edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    <button ><a href="{{ path('promotion_index') }}">Back</a></button>
    {{ form_end(edit_form) }}




{% endblock %}
