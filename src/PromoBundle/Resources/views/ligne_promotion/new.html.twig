{% extends 'adminBase.html.twig' %}

{% block content %}
    <h1>Ligne_promotion creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('ligne_promotion_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
