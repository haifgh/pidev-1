{% extends 'adminBase.html.twig' %}

{% block content %}
<div class="container-fluid mt--7">
    <div class="row">
        <div class="col"></div>
        <div class="card col-6 p-4">
            <div class="card-header">
                <a class="btn btn-outline-dark btn-sm float-right" href="{{ path('guide_index') }}">Back to the list</a>
    <h1>Guide creation</h1>
            </div>
    {{ form_start(form) }}
        {{ form_widget(form) }}
            <center>
        <input class="btn btn-outline-success" type="submit" value="Create" /></center>
    {{ form_end(form) }}




</div><div class="col"></div></div></div>
{% endblock %}
